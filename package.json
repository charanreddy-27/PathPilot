{
  "name": "pathpilot",
  "version": "1.0.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "cd frontend && pnpm run dev",
    "build": "cd frontend && pnpm run build",
    "start": "cd frontend && pnpm start",
    "install:all": "pnpm install && cd frontend && pnpm install --legacy-peer-deps"
  },
  "devDependencies": {
    "@types/node": "^22.15.16"
  }
}
